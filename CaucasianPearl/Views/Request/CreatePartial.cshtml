@using CaucasianPearl.Core.Constants
@using CaucasianPearl.Models.EDM
@using CaucasianPearl.Resources;

@model Request

@using (Ajax.BeginForm(
    Consts.Actions.CreatePartialWithCaptcha,
    Consts.Controllers.Request.Name,
    null,
    new AjaxOptions {
        HttpMethod = "Post",
        OnBegin = "onSendRequestBegin",
        OnComplete = "onSendRequestComplete",
        OnSuccess = "onSendRequestSuccess",
        OnFailure = "onSendRequestFailure"
    },
    new { id = "formSendRequest" })) {

    @Html.Partial(Consts.Views.CreateOrEditPartial, Model)
}